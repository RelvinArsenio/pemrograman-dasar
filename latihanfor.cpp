#include <iostream>
using namespace std;

int main() {
    system("cls");

    for (int i = 1; i <= 30; i++)
    {
        if (i %2 == 0)
        {
           cout << i << " "; 
        }
    }
    
    return 0;
}
